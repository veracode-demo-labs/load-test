PERMISSIONS_ERROR = ("Access denied: Check that the current profile possesses sufficient permissions.\n"
                     "Configure your veracode credentials or use the -id [ID] and -key [KEY] flags with your API credentials\n"
                     "If you have multiple profiles, a profile can be selected with the -profile [PROFILE] flag")